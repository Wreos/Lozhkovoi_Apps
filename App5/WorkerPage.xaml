<Page
    x:Class="Workers.WorkerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Workers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10 0 10 0">
            <TextBlock x:Name="headerBlock" Text = "Добавление сотрудника"
                       Style="{ThemeResource HeaderTextBlockStyle}" />
            <TextBlock>Имя сотрудника</TextBlock>
            <TextBox x:Name="nameBox" PlaceholderText="Меня зовут Саша?" />
            <TextBlock>Фамилия</TextBlock>
            <TextBox x:Name="familyBox" PlaceholderText="" />
            <TextBlock>Количество часов</TextBlock>
            <TextBox x:Name="hoursBox" />
            <TextBlock>Тариф</TextBlock>
            <TextBox x:Name="tarifBox" />
            
            <StackPanel Orientation="Horizontal">
                <Button Content="Сохранить" Click="Save_Click" Margin="10" Background="#FF002EFF" Foreground="White" />
                <Button Content="Отмена" Click="Cancel_Click" Margin="10" Background="#FF002EFF" Foreground="White" />
            </StackPanel>

        </StackPanel>
        
    </Grid>
</Page>
